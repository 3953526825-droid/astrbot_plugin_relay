{
    "random_offset_seconds": {
        "description": "设置定时发送的随机前后浮动时间 (秒)。例如设为 300，则会在设定的目标时间前后 5 分钟内随机触发。设为 0 则严格精确到秒。",
        "type": "int",
        "hint": "定时随机波动范围 (秒)",
        "default": 300
    }
}